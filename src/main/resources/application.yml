spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/demoapp}
    username: ${DB_USER:postgres}
    password: ${DB_PASS:postgres}

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

recaptcha:
  site-key: ${RECAPTCHA_SITE_KEY:6LcAgFIsAAAAAK4hBp3QMpKPrI395edVgRod4nEP}
  secret-key: ${RECAPTCHA_SECRET:6LcAgFIsAAAAAI2qLewcibEh7hEevbyr_Rdkp9nt}

app:
  upload-dir: ${UPLOAD_DIR:./uploads}
